# data and warehouse never enter git
data/
warehouse/
warehouse-k8s/
spark-warehouse/
metastore_db/
derby.log

# environments and secrets
.venv/
.env
*.pem
*.key

# build/runtime junk
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
gx/uncommitted/
# web/dq/ (GX Data Docs) IS committed: it is the published receipt for the
# "DQ gate green" claim on the Pages site; recommit when 05_dq regenerates it

# large binaries (tiles are the exception, committed deliberately in web/)
*.tif
*.tiff
*.gdb
*.zip
*.parquet
!web/*.pmtiles
wh-mvp-final/
publish-scratch/
